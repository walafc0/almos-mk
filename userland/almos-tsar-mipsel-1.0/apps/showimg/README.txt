Make sure to run the TSAR simulator with its default framebuffer configuration:
On the tty1 console of the TSAR simulator, type:

[/HOME/ROOT]> exec /bin/showimg.x -p4 -i /tmp/lena.sgi -o /dev/fb0

Note: -p4 means 4 threads, you can try more!

